# Planning context moved

The canonical context is now [assets/docs/context](../../assets/docs/context/README.md). This compatibility pointer preserves the location referenced by the original GitHub planning issues. Do not create a second copy here.
