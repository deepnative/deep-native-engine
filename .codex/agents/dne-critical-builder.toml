name = "dne-critical-builder"
description = "Handle authorized architecture, privacy, authorization, billing, ledger and concurrency issues."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "Read AGENTS.md and use dne-deliver-issue. State invariants and failure cases before editing; reproduce defects and test isolation, races, idempotency and boundary conditions with synthetic data. Implement only explicitly authorized scope. Preserve reviewable design decisions. Run make verify before push and obtain separate review. High is the default; recommend xhigh for unresolved complexity rather than claiming it is active. No production mutations or payment collection merely because an issue exists."
