# Provisional F-ROADMAP-08-B specialty coverage disclosure

The F-ROADMAP-08-B desktop/mobile browser case uses the compiled local server and disposable PostgreSQL database. Two synthetic verified reviewer entries initially remove the public missing-reviewer gap for Education formal review. Removing verification from both entries restores that gap; the test checks the database rows and public page after a reload. Two synthetic verified coach entries initially have deliverable capacity. Committing all minutes of the primary removes deliverable capacity; the public gap appears after a reload and the test checks the actual capacity row. Both checks assert that the specialty stays in preparation because qualified content is absent, that no booking or purchase control appears, and that private qualification/agreement/conflict references do not appear publicly. The test records its two mandatory subcase markers only after these assertions.

The public gap list is computed from the current local content and expert registry snapshot. It says which evidence is missing for a domain/service without exposing staff identity or private roster references. It is not a capacity promise: no qualified foundation content, actual expert qualification, verified availability, offer approval or tailored booking exists in this preview. The operator registry remains the place to inspect private roster facts. This is a local synthetic disclosure and browser proof, not live service verification.

The case is provisional (`approved:false`); the separate provisional tranche contains 18/18 IDs, 12/12 critical IDs and 36 first-attempt desktop/mobile executions when the gate passes. The approved full-MVP numerator remains 0/100 and the 100-case/94-critical denominator is unchanged. QA-002 #94 and expert-coverage follow-up #96 remain open. Removing this test or its provisional mapping would remove evidence only; the public disclosure behavior would still exist until separately reverted.
